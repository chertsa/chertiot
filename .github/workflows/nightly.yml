name: nightly
on:
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:
permissions:
  contents: read
  packages: read

jobs:
  flood:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.12.7"
      - run: uv --directory portal sync --frozen
      - name: ghcr.io login (pull the published Caddy image)
        uses: docker/login-action@v3
        with: { registry: ghcr.io, username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - run: deploy/scripts/ci-env.sh
      - run: deploy/scripts/write-runtime-secrets.sh
      - run: docker compose --profile core up -d --build
      - run: make wait-healthy
      - run: make bootstrap
        # Throttling is asserted hard; control-device health is a soft-check on the shared runner
        # (starved 2-vCPU host flakes it). Real criterion-4 numbers come from staging `make flood-test`.
      - run: make flood-test
        env:
          FLOOD_STRICT_CONTROL: "0"
