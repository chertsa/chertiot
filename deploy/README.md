# deploy/
VPS provisioning and operations. Filled in M2.2 (staging) and M2.3 (production).
- `scripts/` bootstrap.sh, backup.sh, restore-drill.sh
- `caddy/` Caddyfile (dev routing now; staging/prod vhosts + layer4 MQTTS from M2.2)
- `runbooks/` restore drill report, maintenance window, incident notes
