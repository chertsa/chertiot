# Internal LoRa broker: gateway-bridge ↔ chirpstack, and chirpstack MQTT integration ↔ lora-bridge.
# Not exposed publicly; the flows never leave the compose network.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
