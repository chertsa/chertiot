# firmware-examples/
Student starter code. The portal injects `{{MQTT_HOST}}`, `{{MQTT_PORT}}`, `{{ACCESS_TOKEN}}` into these files at render time (M1.2). Keep templates compilable with placeholder values.
