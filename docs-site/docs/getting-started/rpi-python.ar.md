# Raspberry Pi (أو أي حاسوب لينكس) مع بايثون

**تحتاج إلى:** Python 3.9+ و`pip install paho-mqtt`.

1. نزّل `chertiot_starter.py` من صفحة جهازك.
2. `python3 chertiot_starter.py` — يطبع قراءة كل 10 ثوانٍ.
3. افتح **لوحتي**.

حوّله إلى خدمة عبر `systemd` أو `cron @reboot` بعد أن يعمل كود مستشعرك.
